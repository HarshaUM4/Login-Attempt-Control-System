# Login-Attempt-Control-System
Login Attempt Control System is a Python program that enhances security by limiting incorrect login attempts. After a set number of failed tries, the system temporarily locks access to prevent unauthorized login attempts.
